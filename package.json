{
  "name": "r3-training",
  "description": "R3 Training Platform",
  "version": "2.0.0",
  "hugo-bin": {
    "buildTags": "extended"
  },
  "browserslist": [
    "defaults"
  ],
  "scripts": {
    "build": "hugo --gc --minify",
    "start": "hugo server --disableFastRender",
    "clean": "rm -rf build resources public"
  },
  "devDependencies": {
    "hugo-bin": "^0.76.1",
    "hugo-extended": "^0.88.1"
  }
}
